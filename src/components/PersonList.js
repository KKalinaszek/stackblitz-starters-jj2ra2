import React, {useReducer, useMemo} from "react";

const PersonList = () => {
    return (
        <div>
            {persons.map((person, i) => (
                <p>person.name</p>
            ))}
        </div>
    );
};

export default PersonList;