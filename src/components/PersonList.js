import React from "react";

const PersonForm = () => {
    return (
        <div>
            {persons.map((person, i) => (
                <p>person.name</p>
            ))}
        </div>
    );
};

export default PersonForm;